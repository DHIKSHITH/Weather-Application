sudo: required
services:
  - docker

before_install:
  - docker build -t dhikshith/Weather-Application -f Dockerfile.dev .

script:
  - docker run dhikshith/Weather-Application npm run test -- --coverage
